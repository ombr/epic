#images.container-fluid
  %h1 Studio Cui Cui
  .row
    .col-xs-12
      .images{data: { event: 'event' } }
        - @images.each do |image|
          = link_to image_path(image) do
            = image_tag image.url(:thumbnail), class: 'image', size: '250x200'
